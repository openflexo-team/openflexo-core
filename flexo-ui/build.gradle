dependencies {
  // openflexo dep
  compile relativeProject(':flexo-foundation')
  compile relativeProject(':flexodocresourcemanager')
  compile group: "org.openflexo", name: "connie-core", version: "1.+"
  compile group: "org.openflexo", name: "gina-swing", version: "1.+"
  compile group: "org.openflexo", name: "gina-swing-editor", version: "1.+"
  compile group: "org.openflexo", name: "diana-swing", version: "1.+"
  compile group: "org.openflexo", name: "flexohelp", version: "1.+"
  // external dep

  // testing dep
  testCompile group: "org.openflexo", name: "gina-test", version: "1.+"
  testCompile group: "org.openflexo", name: "testutils", version: "1.+"
  testCompile group: "org.openflexo", name: "gina-swing-editor-test", version: "1.+"
  testCompile relativeProject(':flexo-test-resources')
}

// To be able to use private apple api
compileJava {
    options.compilerArgs << "-XDignore.symbol.file=true"
    options.fork = true
    options.forkOptions.executable = 'javac'
}

test {
  //testLogging {
  //   showStandardStreams = true
  //}
    //    include '**/TestPreferences*'// > testLoadViewPointFromAClassPathJar
    //    exclude '**/TestLoadViewPoint*'// > testLoadViewPointFromAClassPathJar
}
