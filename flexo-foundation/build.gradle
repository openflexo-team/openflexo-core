dependencies {
  // openflexo dep
  compile flexoP2PP();
  compile flexoLocalization()
  // Still to be removed
  compile flexoGraphicUtils() // used for icon mgt in FlexoConcept
  //compile diana() // used by FMLModelFactory which inherit from DianaModelFactoryImpl

  // external dep
  compile group: 'org.eclipse.jgit', name: 'org.eclipse.jgit', version: '4.3.1.201605051710-r'
  compile group: 'com.google.inject', name: 'guice', version: '4.1.0'
  compile group: 'findbugs', name: 'annotations', version: '1.0.0'
  compile group: 'xerces', name: 'xercesImpl', version: '2.11.0'
}

if (JavaVersion.current().isJava9Compatible()) {
	tasks.withType(JavaCompile) {
		options.compilerArgs.addAll(['--add-modules', 'java.xml.bind'])
    }
}  
